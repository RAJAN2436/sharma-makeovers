services:
  - type: web
    name: sharma-makeovers
    env: node
    rootDir: .
    buildCommand: npm run build:client && npm run build:server
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
